---
description: "Parse a HAR file and extract API endpoint summary"
---

Run the HAR parser on the given file path and display the extracted endpoints, schemas, and summary.

```
uv run python -m pathhar.parsing.har_parser $ARGUMENTS
```
